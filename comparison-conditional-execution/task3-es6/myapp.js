var varSymbol = "Var symbol here";
let letSymbol = "Let symbol here";
const letSymbol = "const symbol here";
/*
var : if - Globally
      for - Globally
      function - locally 
      reAssignment - yes
      update(add,delete) - yes

let : if - Locally
      for - Locally
      function - Locally
      reAssignment - yes
      update(add,delete) - yes

const : if - Locally
        for - Locally
        function - Locally
        reAssignment - no
        update(add,delete) - yes
*/    